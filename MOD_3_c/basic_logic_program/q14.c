// Find ascii value of given number

#include <stdio.h>

int main() {
    int ascii;

    printf("Enter the ASCII no: ");
    scanf("%d", &ascii);

    printf("Char value of '%d' is '%c'",ascii,ascii);

    return 0;
}
